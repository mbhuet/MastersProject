using UnityEngine;
using UnityEngine.UI;

public class BluetoothLobbyCanvasHooks : MonoBehaviour
{
	/*
	public GameObject playerPanelPrefab;
	
	public delegate void CanvasHook();

	PlayerPanelHooks[] panelHooks;

	
	void Start(){
		panelHooks = new PlayerPanelHooks[4];
		for (int i = 0; i< 4; i++) {
			GameObject panel = (GameObject)GameObject.Instantiate(playerPanelPrefab);
			panel.transform.SetParent(transform.GetChild(0));
			//panel.transform.SetPare = transform.GetChild(0);
			panelHooks[i] = panel.GetComponent<PlayerPanelHooks>();
		}
	}

	public void SetPlayer(int i, PlayerLobby player){
		panelHooks [i].SetPlayer (player);
		
	}

	public void RemovePlayer(int i){
		if (i >= panelHooks.Length)
			return;

		panelHooks [i].RemovePlayer ();

	}
	*/
	
}
